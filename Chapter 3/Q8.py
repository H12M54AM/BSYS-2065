# Hand in 
"""
Write a program that will compute the area of a circle. Prompt the 
user to enter the radius and print a nice message back to the user 
with the answer.
"""

PI = 3.14
r = float(input("What is the Radius?: "))
print(f"Area = {PI*r**2}")